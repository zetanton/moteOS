[package]
name = "inference"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["lib"]

[dependencies]
# Core dependencies for no_std
alloc = { version = "1.0", package = "alloc" }

[features]
default = []
no_std = []
